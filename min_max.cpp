/**
 * Objective: 
 * - Return the minimum and maximum balue from an unsorted vector
 *
 * Input: vector of numbers (unsorted)
 *
 * Output: print both the minimum and maximum values found in the list
 */

#include <iostream>
#include <vector>
using namespace std;

void print_min_max(vector<int> unsorted_list) {
    // TODO: add implementation here
    
    // cout << "MAX: " << ? << std::endl; 
    // cout << "MIN: " << ? << std::endl;
}

auto main() -> int {
    vector<int> unsorted_list = { 9, 2, 3, 4, 5, 10, 7, 8, 1, 6, 21, 998, 0, -1 };
    print_min_max(unsorted_list);
}
